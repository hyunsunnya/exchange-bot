name: Ticket & ETF Notification Bot

on:
  workflow_dispatch:  # 수동 실행 시 3개 파일 모두 작동
  schedule:
    - cron: '0 2 * * *'   # 한국 시간 오전 11:00 (티켓 알림용, UTC 02:00)
    - cron: '0 4 * * *'   # 한국 시간 오후 1:00 (ETF 알림용, UTC 04:00)

jobs:
  job_run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests beautifulsoup4 python-telegram-bot yfinance pandas

      - name: Run Scripts based on Time
        env:
          KMA_API_KEY: ${{ secrets.KMA_API_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          # 현재 시간(UTC)의 '시(Hour)' 정보 추출
          HOUR=$(date +%H)
          
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "🚀 수동 실행: 모든 스크립트를 가동합니다. (test, etf, ticket)"
            python test.py || echo "test.py failed"
            python etf_tracker.py || echo "etf_tracker.py failed"
            python ticket_gh.py || echo "ticket_gh.py failed"
            
          elif [ "$HOUR" == "02" ]; then
            echo "🎫 오전 11시: 티켓 알림을 실행합니다."
            python ticket_gh.py
            
          elif [ "$HOUR" == "04" ]; then
            echo "📊 오후 1시: ETF 시세 알림을 실행합니다."
            python etf_tracker.py
          fi
